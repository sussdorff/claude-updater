__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.venv/
.pytest_cache/
.ruff_cache/
*.egg

# Dolt database files (added by bd init)
.dolt/
*.db

# Beads local state (not shared)
.beads/metadata.json
.beads/dolt-server.port
.beads/dolt-server.*
.beads/dolt-monitor.*
.beads/dolt/
.beads/backup/
.beads/interactions.jsonl
.beads/last-touched

# Local Claude commands (credentials, project-specific overrides)
.claude/commands/

# Environment (may contain credentials)
.env
