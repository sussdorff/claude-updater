# Session Log 2026-03-30

## Session End

### Topics
- Fix: `importlib.metadata` Version-Konflikt zwischen uv-tool-Install und editable Dev-Install
- Windows-Support: `download_gh_release_binary()` für beads/dolt
- Adapter-Refactoring: beads_cli.py, dolt.py auf shared `download_gh_release_binary` umgestellt
- Encoding-Fix: `read_text(encoding="utf-8")` in base.py, beads_plugin.py, claude_mem.py

### Decisions
- `get_installed_version()` fragt zuerst `uv tool list` statt `importlib.metadata` — vermeidet Dev-Install-Konflikt
- Windows-Updates für beads/dolt via GitHub Releases + zip-extract statt winget/PowerShell-Installer

### Open Threads
- ~~Änderungen auf `feat/system-prompt-adapter` noch nicht committed/gemergt~~ → committed + gepusht

### Action Items
- [x] Commit + Push der Adapter-Änderungen
- [ ] Branch mergen oder PR erstellen

### Files Changed
- `.gitignore` — `.claude/commands/` und `.env` ignorieren
- `src/claude_updater/adapters/base.py` — `download_gh_release_binary()` + `_download_file()` hinzugefügt, encoding-fix
- `src/claude_updater/adapters/beads_cli.py` — Windows-Update via GitHub release statt PowerShell
- `src/claude_updater/adapters/beads_plugin.py` — encoding-fix
- `src/claude_updater/adapters/claude_mem.py` — encoding-fix
- `src/claude_updater/adapters/claude_updater.py` — Version via `uv tool list` statt `importlib.metadata`
- `src/claude_updater/adapters/dolt.py` — Windows-Update via GitHub release statt winget

## Session End 13:30

### Topics
- Dolt remote push debugging: `bd dolt push` scheiterte mit "Client sent HTTP to HTTPS server"
- Root cause: Dolt 1.83.5 generiert Upload-URLs mit `http://` statt `https://` hinter Reverse Proxy
- Server-Infrastruktur: Caddy Caddyfile, Docker-Compose für dolt-remote und caddy
- beads plugin Update 0.62.0 → 0.63.2

### Decisions
- Dolt-remote auf `dolthub/dolt:latest` (1.84.0) upgraded — enthält Fix für X-Forwarded-Proto bei Upload-URLs
- Port 50051 vom dolt-remote Docker entfernt, stattdessen Caddy auf :50051 mit TLS
- Caddy-Config: `dolt.adrianphilipp.de, dolt.adrianphilipp.de:50051` mit h2c-Proxy

### Open Threads
- Claude Code 2.1.86 → 2.1.87 Update ausstehend (Restart nötig)
- `feat/system-prompt-adapter` Branch noch nicht nach main gemergt

### Action Items
- [ ] Claude Code neustarten für 2.1.87
- [ ] PR für feat/system-prompt-adapter erstellen

### Files Changed
- Server: `/home/adrian/docker/caddy/Caddyfile` — Port 50051 + debug log entfernt
- Server: `/home/adrian/docker/caddy/docker-compose.yml` — Port 50051 mapping
- Server: `/home/adrian/apps/dolt-remote/docker-compose.yml` — Image auf latest, Port-Mapping entfernt
