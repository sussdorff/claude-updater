# Session Log 2026-03-31

## Session End

### Topics
- Fix: PermissionError beim dolt.exe Update auf Windows (gesperrte Datei)
- Fix: Update-Loop Crash-Resilienz — ein fehlgeschlagenes Tool crasht nicht mehr den gesamten Prozess

### Decisions
- Windows-Binaries vor Update umbenennen statt direkt überschreiben (rename-before-copy pattern)
- Exception-Handler um `apply_update()` im Runner — Cache wird auch bei Teil-Failures invalidiert

### Open Threads
- `feat/system-prompt-adapter` Branch noch nicht nach main gemergt
- `check` zeigt nach partiellem Update noch alte Versionen (Cache-Problem durch Crash behoben)

### Action Items
- [ ] PR für feat/system-prompt-adapter erstellen
- [ ] Testen ob dolt.exe Update nach Fix funktioniert

### Files Changed
- `src/claude_updater/adapters/base.py` — rename-before-copy für gesperrte Windows-Binaries, PermissionError handling
- `src/claude_updater/runner.py` — try/except um apply_update() im Update-Loop
